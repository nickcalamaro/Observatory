
name: Deploy Bunny Scripting Script

on:
  push:
    branches:
      - master

permissions:
  id-token: write
  contents: read

jobs:
  update-script:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Publish Charts Script to Bunny
        uses: "BunnyWay/actions/deploy-script@main"
        with:
          script_id: 64649
          file: "scripts/bunnyChartsScript.js"
